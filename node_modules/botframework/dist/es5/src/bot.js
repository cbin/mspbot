"use strict";
var facebook_1 = require('./facebook');
var Bot = (function () {
    function Bot(settings, botController) {
        this.settings = settings;
        this.botController = botController;
        this.facebookBot = null;
        this.facebookServer = null;
        if (settings.fb) {
            this.facebookBot = new facebook_1.FacebookBot(settings, botController);
            this.facebookServer = new facebook_1.FacebookServer(settings, this.facebookBot);
        }
    }
    Bot.prototype.setWelcomeMessage = function (text) {
        if (!this.facebookServer)
            return;
        this.facebookServer.setWelcomeMessage(text);
    };
    return Bot;
}());
exports.Bot = Bot;
//# sourceMappingURL=bot.js.map