import { IBotSettings } from '../interfaces';
import { FacebookBot } from './bot';
export declare class FacebookServer {
    private settings;
    private facebookBot;
    constructor(settings: IBotSettings, facebookBot: FacebookBot);
    setWelcomeMessage(text: string): void;
    private startWebServer();
    private receiveMessage(request, reply);
    private verifyRequest(request, reply);
    private subscribe();
}
