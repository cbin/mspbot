import { IBotSettings } from '../interfaces';
import { IFbResponse } from './interfaces';
import * as Promise from 'bluebird';
export interface IFacebookProfile {
    first_name?: string;
    last_name?: string;
    profile_pic?: string;
    locale?: string;
    timezone?: number;
    gender?: string;
}
export declare class FacebookApi {
    private settings;
    constructor(settings: IBotSettings);
    sendMessage(msg: IFbResponse): Promise<any>;
    getUserDetails(userId: number): Promise<IFacebookProfile>;
    subscribe(): void;
}
